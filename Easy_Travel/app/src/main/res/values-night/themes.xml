<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Easy_travel.Dark" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">#424242</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">#424242</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">#424242</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Light mode -->
    <!-- Screen background if light mode on -->
    <style name="SCREEN">
        <item name="android:background">#424242</item>
        <item name="android:color">#7C7C7C</item>
    </style>

    <!-- Texts background if light mode on -->
    <style name="TEXT">
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- button background if light mode on -->
    <style name="BUTTON">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/purple_700</item>
    </style>

    <style name="LINE">
        <item name="android:fillColor">@color/white</item>
        <item name="android:strokeColor">@color/white</item>
        <item name="android:tint">@color/white</item>
        <item name="android:background">@color/white</item>
    </style>

</resources>